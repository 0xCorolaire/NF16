#define PI 3.14 // A la phase de preprocessing, les ref dans le code à PI seront remplacées par 3.14

int main() {
    int rayon = 10; // Initialisation
    float resultat = PI*rayon*rayon;
    return 0;
}
